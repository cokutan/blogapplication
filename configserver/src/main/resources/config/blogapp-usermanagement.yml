server:
  port: 9010
keycloak:
  auth-server-url: http://localhost:8888/auth
  realm: blogapp
  client-id: blogapp
  client-secret: FsZL17sZd2cSp7bSnCrDdIcS6yuqefC6

spring:
  application:
    name: blogapp-usermanagement
  jpa:
    defer-datasource-initialization: 'true'
    open-in-view: 'false'
    database-platform: org.hibernate.dialect.MySQLDialect
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    password: '123456'
    username: cokutan
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3307}/blogapp
  profiles:
    active: production

  #Publish Application(Register with Eureka)
  eureka:
    client:
      service-url:
        default-zone: http://localhost:8761/eureka
  # instance id for eureka server
    instance:
      instance-id: ${spring.application.name}:${random.value}

  logging.level:
    org.springframework.web.server.adapter.HttpWebHandlerAdapter: TRACE